interface:
  display_name: Principle Migrate Callers Then Delete Legacy Apis
  short_description: Invoke the pstack principle-migrate-callers-then-delete-legacy-apis
    workflow.
policy:
  allow_implicit_invocation: false
